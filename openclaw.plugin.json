{
  "id": "ets",
  "name": "ETS \u2014 Email Token Saver",
  "description": "Rules-based email pre-filter. Strips noise, extracts structure, and categorizes intent before the LLM sees a single byte.",
  "version": "1.4.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "rulesPath": {
        "type": "string",
        "description": "Path to email_rules.json"
      },
      "dbPath": {
        "type": "string",
        "description": "Path to SQLite DB"
      },
      "localRulesPath": {
        "type": "string",
        "description": "Path to local rules overrides file (merged with bundled rules, overrides by ID). Gitignored — for personal/private rules."
      },
      "templatesPath": {
        "type": "string",
        "description": "Path to extractor_templates.json"
      },
      "blockThreshold": {
        "type": "number",
        "default": -50
      },
      "allowThreshold": {
        "type": "number",
        "default": 50
      },
      "snippetCap": {
        "type": "number",
        "default": 300
      }
    }
  }
}